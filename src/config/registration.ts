/**
 * External registration forms, one per event.
 *
 * The hero CTA, the closing CTA and the sticky widget all point at the same
 * URL, so it is declared once here rather than three times across a feature.
 */
// TODO: swap the placeholders for the real form URLs.
export const registrationUrls = {
  seminar: "/",
  youthEntrepreneurSummit: "/",
} as const;
